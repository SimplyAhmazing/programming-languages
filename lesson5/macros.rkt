#lang racket

; A macro definition describes how to transform new syntax into
; different syntax in the source language. A macro is one way to
; implement syntactic sugar

; A macro system is a language (part of a larger language) for defining macros

; Macro expansion is the process of rewriting all the macro definitions from the
; programmes code

